name: Welcome Room
on:
  push:
    branches: [main]
    paths:
      - "WELCOME/request.txt"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run Welcome
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python WELCOME/scripts/welcome_encounter.py

      - name: Commit response
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "museum-bot"
          git config user.email "museum-bot@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          git add WELCOME/response.txt
          git add WELCOME/visits.json
          git add WELCOME/history.json
          git add museum-state.json
          git add journey/
          git diff --staged --quiet || git commit -m "Welcome: lobby visited"
          git pull --rebase -X theirs origin main
          git push origin main